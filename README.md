# ECO
Unofficial implementation ECO.

## python ECO.py --help
```
usage: ECO.py [-h] [--videos VIDEOS] [--weights_dir WEIGHTS_DIR]
              [--resize RESIZE] [--crop_size CROP_SIZE]
              [--num_segments NUM_SEGMENTS] [--lr LR]
              [--batch_size BATCH_SIZE] [--epochs EPOCHS] [--cpu] [--noresume]
              [--predict PREDICT] [--validate]

options:
  -h, --help            show this help message and exit
  --videos VIDEOS
  --weights_dir WEIGHTS_DIR
  --resize RESIZE
  --crop_size CROP_SIZE
  --num_segments NUM_SEGMENTS
  --lr LR
  --batch_size BATCH_SIZE
  --epochs EPOCHS
  --cpu
  --noresume
  --predict PREDICT
  --validate
```
